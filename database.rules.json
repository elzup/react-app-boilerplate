{
	"rules": {
		"users": {
			".read": "auth !== null",
			".write": "auth !== null",

			"$user_id": {
				".write": "auth.uid === $user_id",

				"potatos": {
					".read": "auth.uid === $user_id"
				}
			}
		},
		"potatos": {
			".read": true,
			".write": "auth !== null"
		}
	}
}
